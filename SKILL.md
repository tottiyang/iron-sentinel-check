# 铁血哨兵 v2 - A股买点审核系统

> ⚠️ **唯一调用入口**：所有 Agent 和新开会话审核股票时，必须调用本 Skill。旧版 `iron-sentinel` 和 `check_buy_point.py` 已废弃。
> 
> 📁 **代码位置**：`~/.qclaw/skills/iron-sentinel/`

---

## 触发关键词（匹配以下任意表述即调用本 Skill）

**股票代码/名称类**（直接审核）：
- 审核 `鹏辉能源`、`300438`、`sz300438`、`sh600519` 等任意股票
- 帮我审核 `xxx`
- 审核一下 `xxx`
- 审核 + 股票名称/代码

**买点判断类**（意图审核）：
- 帮我看看这只股票能不能买 / 能不能买入
- 这只股票现在可以买吗
- 帮我判断一下买点
- 现在是买点吗 / 买点到了吗
- 帮我分析一下这只股票的买点
- 买点审核 / 买点判断 / 买点检查

**铁血哨兵**（直接调用）：
- 铁血哨兵 `300438`
- 铁血哨兵审核 `xxx`
- 用铁血哨兵审核

---

## 简介

**铁血哨兵** 是 A股买点智能审核系统，基于 11 项严格标准判断股票买点时机，综合评估后给出买入/观望/不推荐建议。

---

## 核心审核维度（11项）

| # | 审核项 | 权重 | 通过条件 |
|---|--------|------|---------|
| 1 | MACD动能增强 | 10 | 红柱增长 或 绿柱缩短 |
| 2 | 分时均线上方 | 10 | 现价 ≥ 均价 |
| 3 | 量能放大 | 10 | 今日成交量 > 近5日均量×1.1 |
| 4 | 上升趋势 | 10 | MA5 > MA10 > MA20 且价格在MA5上方 |
| 5 | 主力净流入 | 10 | 超大单+大单净流入 > 0 |
| 6 | 基本面良好 | 10 | PE 0~150 且 PB < 10 |
| 7 | 大盘日内非下行 | 5 | 上证或深证涨跌幅 > -0.5% |
| 8 | 大盘趋势非下行 | 5 | 上证指数 > MA5 |
| 9 | 板块趋势向上 | 5 | 所属板块涨幅 > 0 |
| 10 | 龙头活跃 | 12 | 近5日正增≥3/5 且 今日盘中正增≥3/5 |
| 11 | 筹码集中度 | 13 | 股东户数环比<0 且 前十流通股≥30% |

**综合评分**：通过项权重相加，满分 100 分
- ≥80 分：🟢 优秀买点
- 70-79 分：🟡 较好买点
- 50-69 分：⚠️ 一般买点
- 30-49 分：🔴 较差买点
- <30 分：⚫ 极差买点

---

## 三层架构（数据 → 分析 → 展示）

```
engine.py ──→ analyze.py ──→ build_report.py
 采集层       分析层         展示层
```

- **engine.py**: 数据采集，输出结构化 JSON（含 SHA256 数据指纹防篡改）
- **analyze.py**: 智能分析，支持规则引擎或 LLM API（可替换）
- **build_report.py**: 固定格式展示，所有模型输出统一表格样式

## 使用方式

### 方式一：直接导入（推荐，Agent 标准调用方式）

```python
from engine import IronSentinelEngine
from build_report import build_from_report

# 获取数据
report = IronSentinelEngine('300438').audit()

# 生成格式化报告
print(build_from_report(report))
```

### 方式二：命令行

```bash
cd ~/.qclaw/skills/iron-sentinel/scripts

# 简洁输出（仅评分）
python3 engine.py 300438

# JSON 输出（供下游分析）
python3 engine.py 300438 --json

# 完整格式化报告（推荐）
python3 engine.py 300438 --format
```

### 方式三：审核任意代码格式

```python
IronSentinelEngine('300438').audit()    # 数字
IronSentinelEngine('sz300438').audit()  # 带市场前缀
IronSentinelEngine('sh600519').audit()  # 上证
IronSentinelEngine('688717').audit()    # 科创板
```

---

## 数据源策略（三层降级，永不裸奔）

```
优先: NeoData（质量最高）
  ↓ 失败 → 备用: 腾讯/新浪（实时行情）
      ↓ 失败 → 降级: Akshare（免费兜底）
          ↓ 失败 → 标记"数据不可用"（不影响其他项）
```

- 每个数据源失败时明确标记，不静默跳过
- 数据可用率正常应达到 7-9/9 项
- **量能单位（⚠️ 多次踩坑，2026-04-13 确认）**:
  - 科创板(688xxx): NeoData/腾讯返回"股"，需÷100转手
  - 创业板(300xxx)、主板沪市(603xxx): NeoData/腾讯返回"手"，直接使用
  - 新浪K线历史: 已在 data_source.py 中转为手
  - 原理: amount(万元) / price(元) * 100 = 理论手数，可用于验证

---

## 评分等级说明

| 等级 | 分数 | 含义 | 操作建议 |
|------|------|------|---------|
| 🟢 优秀买点 | ≥80 | 各项指标健康 | 强烈建议买入 |
| 🟡 较好买点 | 70-79 | 大部分指标达标 | 可以考虑买入 |
| ⚠️ 一般买点 | 50-69 | 条件参差 | 少量试探 |
| 🔴 较差买点 | 30-49 | 风险项多 | 建议观望 |
| ⚫ 极差买点 | <30 | 多项不符 | 耐心等待更好时机 |

---

## 重要提示

1. **本系统仅供参考**，不构成投资建议
2. **请到海通证券确认 KD 指标**后再做最终决策
3. **大盘环境不好时**，即使个股评分高也应谨慎
4. **数据不可用项越多**，评分可靠性越低

---

## 文件结构

```
iron-sentinel/
├── SKILL.md              # 本文件（使用说明）
└── scripts/
    ├── __init__.py
    ├── engine.py         # 主引擎（数据采集层）
    ├── data_source.py    # 数据源层（三层降级）
    ├── checks.py         # 审核函数（11项独立函数）
    ├── analyze.py        # 智能分析层（规则引擎/LLM）
    └── build_report.py   # 展示层（统一表格格式）
```

---

## 调用约定（所有 Agent 必须遵守）

审核任意股票时，直接导入 `IronSentinelEngine`：

```python
审核 <股票名称/代码> → 调用 IronSentinelEngine('<代码>').audit()
```

禁止：
- ❌ 调用 `check_buy_point.py`（已废弃）
- ❌ 调用 skills/iron-sentinel 旧目录（已删除）
- ❌ 调用 workspace 中的旧版脚本

## 数据完整性保护

- **SHA256 指纹**: engine.py 输出时对 `results` + `leader_details` 生成指纹
- **防篡改校验**: analyze.py 和 build_report.py 入口校验指纹，被篡改则拒绝处理
- **原始数据锁定**: 展示层表格数据直接来自 `raw_data`，不得修改

## 非交易日处理

- 周末/节假日自动使用**最后交易日数据**
- `_effective_now()` 强制走"盘后模式"
- 量能审核使用历史数据参与计算

---

---

## ⚠️ 踩坑记录（永久保留）

### 2026-04-13 量能单位反复修改问题

**问题**: 腾讯/NeoData 的 vol 字段对不同板块单位不一致，导致量能数据错误。

**反复过程**:
1. 最初：认为 NeoData 统一是"手"，不处理
2. 第一次改：NeoData 全都÷100 → 688717 变成 713万手（错误）
3. 回滚：不÷100 → 300438/603919 变成几十手（错误）
4. 根因分析：发现 **科创板(688xxx)是"股"单位**，创业板/主板是"手"单位
5. 最终修复：根据代码前缀判断板块，分别处理

**教训**:
- 同一字段在不同板块可能完全不同单位
- 永远不要假设"这个数据源统一是XX单位"
- 验证方法：amount(万元) / price(元) × 100 = 理论手数

**正确代码**:
```python
is_kcb = code.lower().startswith(('sh688', 'sz688'))
vol_hand = vol_raw / 100.0 if is_kcb else vol_raw
```

### 2026-04-14 data_source.py 腾讯vol处理错误

**问题**: `get_realtime_tencent()` 中 `vol = float(f(36, 0)) * 100`，错误地把腾讯vol × 100。

**根因**: 
- 腾讯字段36的单位：科创板=股，创业板/主板=手
- 代码假设"手→股"转换，但创业板/主板返回的本身就是"手"
- 导致创业板vol被放大100倍（9万手→900万手）

**修复**: 
- 移除 `* 100`，直接返回原始值
- 单位转换由 `engine._inject_today_into_bars()` 根据板块判断处理

**验证**: 
- 300693创业板：9.47万手 ✅（修复前890万手）
- 603919主板：1.06万手 ✅
- 688717科创板：5.25万手 ✅

---

*版本：v2.3 | 更新：2026-04-14 | 路径：`~/.qclaw/skills/iron-sentinel/`*
